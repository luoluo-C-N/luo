# Kirameku 管理App · 项目速览（给新加入的协作者）

> 本文件由 ZCode 维护，是给协作者（Astra 等）的一页式项目简报。大改动同步到这里，讨论去 `聊天室.md`。

## 一句话
为个人网站「洛洛の小站 (Kirameku)」做一个手机管理 App：监控服务器、管理内容（文章/说说/相册/音乐/友链…）、审核评论留言、高度自定义的模块化面板工作台。

## 三方分工（2026-09-06 01:18 聊天室确定）
| Agent | 负责范围 |
|---|---|
| **ZCode（会话③）** | 后端安全收尾、全局进度汇总、向用户汇报 |
| **ZCode（原型侧）** | `ui-design` 原型 v2 CRUD、说说评论审核、编辑器增量 |
| **Astra (Codex)** | `appkf` 客户端壳/模块化拆分/PWA/Android 构建/验收 |

## 路径地图
| 资产 | 路径 | 说明 |
|---|---|---|
| App 交互原型 | `E:\glm\ui-design\admin-app.html` | 单文件 ~180KB，全部交互+样式，改完浏览器即验 |
| **v2 原型（拆分基准）** | `E:\glm\appkf\reference\admin-app.v2-crud.html` | 内容 CRUD 九类已接真后端 |
| 客户端工程 | `E:\glm\appkf\`（src/ index.html tests/） | Capacitor + node:test + Playwright |
| **APK 产物** | `E:\glm\appkf\release\kirameku-0.01-debug.apk` | debug 签名，已过 v2 签名校验 |
| 设计规范 | `E:\glm\ui-design\管理App设计规范.md` | 架构/组件/字段字典/验证记录 |
| 网页设计规范 | `E:\glm\ui-design\设计规范.md` | 前台网站的 Tokens |
| 壁纸素材 | `E:\glm\ui-design\wallpapers\` | App 背景壁纸 |
| 后端 | `C:\Users\洛洛\.zcode\workspace\default\Kirameku\Kirameku-backend` | FastAPI, SQLite |
| 前台网站 | `C:\Users\洛洛\.zcode\workspace\default\Kirameku\Kirameku` | Next.js 3000 |
| 契约测试 | `E:\glm\appkf\tests\backend-contracts.py` | 8011 隔离后端 + fixture，17 项 |
| 契约文档 | `E:\glm\appkf\docs\api-contracts.md` | Astra 的源码级 API 审计 |

## 后端速用
```bash
cd Kirameku-backend
.venv/Scripts/python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
# 登录 admin/admin123 → POST /api/auth/login → {code:0,data:{accessToken}}
# 所有 /api/* 返回 {code, message, data} 包裹
# ⚠️ uvicorn 无 --reload：改完代码必须手动重启 8000 才生效
```

## 后端安全现状（2026-09-06 01:22 起，ZCode 会话③维护）
- 管理写接口一律 `Depends(get_admin_user)`（`app/utils/auth.py`，校验 JWT `admin` claim）——GitHub OAuth 普通用户 token 调管理接口一律 403「需要管理员权限」，匿名 401/403
- 公开读接口、公开评论/留言创建不受影响；`GET /api/auth/me` 对任意登录用户合法（仍用 `get_current_user`）
- 管理员审核查询覆盖嵌套回复（pending 筛选不再漏子级）；`DELETE /api/visitors`（清空）已补鉴权
- ⚠️ 事故记录（01:19）：硬化生效前，8000 旧代码进程仍在运行，验证用的 `DELETE /api/visitors` 请求真实清空了访客记录（Kirameku-backend 下无备份）。内容数据（文章/评论/留言/音乐）未受影响。教训见协作规则第 5 条。

## 回归命令（任何人改完后端/客户端后必跑）
```bash
# 1) appkf 单测 + 构建
cd E:\glm\appkf && npm test && npm run build
# 2) 契约回归：先起隔离后端（每次从源码全新拷贝，凭据写入 artifacts/test-backend/fixture.json；
#    若 8011 已有旧实例先 taskkill，否则登录会对不上）
cd appkf\scripts && set KIRAMEKU_BACKEND_SOURCE=C:\Users\洛洛\.zcode\workspace\default\Kirameku\Kirameku-backend && ..\..\.venv 占位——用 backend 的 .venv\Scripts\python.exe 运行 test-backend.py
cd E:\glm\appkf && python tests\backend-contracts.py   # 期望 17/17 PASS
# 3) 重启 8000 正式后端（见「后端速用」）
```

## API 模块清单（后端已全部就绪）
auth / posts / comments(审核) / chatters(说说+其评论审核) / messages(留言审核) / albums+photos / categories / tags / projects / friend_links / bookmarks(收藏夹+sites) / visitors / music(上传50MB) / system(pservice状态) / dashboard(stats) / site_config / upload(图片) / github_auth(OAuth)

## App 原型当前状态
- ✅ 多面板工作台（面板=页面，可新建/关闭/重命名/置顶/排序）
- ✅ 自编译模块平台（6 模板形态 + 代码模式 sandbox iframe + host.data/host.refresh 桥 + 分享码）
- ✅ CodeMirror 编辑器（htmlmixed/css/js，深色配色）
- ✅ 真实数据：系统状态轮询、音乐管理、评论+留言审核闭环
- ✅ GitHub OAuth 前端全流程（待用户配置 CLIENT_ID）
- ✅ 深色模式 / 重命名体系 / 左缘抽屉手势 / 卡片尺寸切换
- ✅ 内容 CRUD 层 v2（2026-09-06）：文章/说说/相册/分类/标签/项目/友链/收藏夹(双层)/访客 全部接真实后端，浏览器全链路验证（发布→前台 API 即时可见、增删改查往返、后端零残留）
- 📦 交接：最新原型已拷贝至 `reference/admin-app.v2-crud.html`，变更清单见聊天室 2026-09-06 01:30 消息
- 🔧 工程侧（Astra）：Capacitor Android 壳 + src/ 模块化拆分 + node:test/Playwright 测试，契约见 `docs/api-contracts.md`

## 当前负责与协作规则
- **Astra 已退出**，工程（kirameku-pocket）由 ZCode 独立维护；聊天室留档回溯。
- 同步流程：改 `E:\glm\ui-design\admin-app.html`（主副本）→ `node tools/split-latest.mjs` → `npm run build` → 冒烟。

## 协作规则
1. 改原型前先读 `管理App设计规范.md` 对应章节，遵守毛玻璃配方（第 6 节）。
2. 每个功能改动 = 代码 + 浏览器 DOM 断言（截图看视觉）；验证记录写进设计文档。
3. **单一写者原则**：原型是单文件，并行编辑会互相覆盖——改之前在聊天室声明要动的区域；`Kirameku-backend` 同一时间只允许一个 agent 写，改完在聊天室回报。
4. 讨论在 `聊天室.md` 追加消息；本文件只放「当前事实」。
5. **破坏性接口的验证只允许打 8011 隔离 fixture，严禁打 8000**（01:19 事故教训）。
6. 改完后端必须重启 8000 并在聊天室声明，避免他人测试打到旧代码。
