# kirameku 掌上小站 · 真机验证用例清单

**版本**：v0.3.0（commit d95772b） · **产出日期**：2026-09-10
**适用**：Android 真机（Android 8.0+） · **预计执行时长**：60–90 分钟
**被测 APK**：`release/kirameku-0.3.0-debug.apk`
**SHA256**：`e09e5990755992ec2b9127bc6963e8b9ed1e0bde7bcb07abc0ff41165ee4dd2e`

---

## 〇、执行前准备（准入条件）

| 项 | 要求 |
|---|---|
| 设备 | Android 真机，已开启「开发者选项 → USB 调试」 |
| 后端 | 已部署且手机可访问（同一局域网；`我的 → 连接设置` 填后端地址） |
| 账号 | 至少一个管理员账号（用于审核/服务管理等需鉴权功能） |
| 网络 | ① 正常 WiFi ② 可断开的数据网络（测弱网用例） |
| 环境隔离 | 建议先「卸载重装」保证初始状态干净 |

**辅助工具**（可选，脚本已修好）：

```powershell
# 查看设备信息（型号/系统/分辨率）
powershell -File scripts/device-qa.ps1 -Action info
# 安装最新 APK（脚本自动选 release 下最新）
powershell -File scripts/device-qa.ps1 -Action install
# 清空 App 数据（回到初始状态）
powershell -File scripts/device-qa.ps1 -Action reset
# 启动 App
powershell -File scripts/device-qa.ps1 -Action launch
# 截图存档（证据留证）
powershell -File scripts/device-qa.ps1 -Action screenshot -Name case-A1
# 抓日志（崩溃/ANR 排查）
powershell -File scripts/device-qa.ps1 -Action logs
# 性能数据（启动耗时/内存）
powershell -File scripts/device-qa.ps1 -Action perf
```

> **自动化覆盖说明**：本清单全部为**手工用例**。已由自动化覆盖的部分（单元 126 项、E2E 浏览器 21 项、联调 E2E 10 项、接口契约 17 项）不在此重复；本清单聚焦**只有真机能暴露**的问题——触摸手势、系统 WebView 渲染差异、安全区/刘海适配、弱网与中断、安装升级、性能表现。

---

## A. 安装与升级（P0）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-INST-001 | P0 | 首次安装 | 设备无本应用 | 1. `adb install` 安装 APK | 安装成功，桌面出现图标，图标清晰无锯齿 |
| TC-INST-002 | P0 | 首次启动 | 已安装未启动 | 1. 点击图标 | 3 秒内出现界面；不白屏、不闪退 |
| TC-INST-003 | P0 | 覆盖安装 | 已装同版本 | 1. 再次安装同 APK | 安装成功；原有数据（登录态/配置）保留 |
| TC-INST-004 | P1 | 升级安装（跨版本） | 已装 0.2.0 | 1. 安装 0.3.0 | 升级成功；localStorage 数据保留；`我的`页版本号显示新版本 |
| TC-INST-005 | P1 | 卸载重装 | 已安装 | 1. 卸载 2. 重装 3. 启动 | 数据清空、回到初始态，不残留旧缓存导致异常 |
| TC-INST-006 | P2 | 安装包校验 | — | 1. 校验 APK 签名与 SHA256 | 签名 v2 有效；SHA256 与上文一致 |

## B. 启动与首屏（P0）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-BOOT-001 | P0 | 冷启动首屏 | 已登录、后端正常 | 1. 杀进程 2. 启动 | 看板正常渲染：三格统计有数值、快捷入口、趋势图、待办、分区开关 |
| TC-BOOT-002 | P0 | **不强制登录** | 已登出 | 1. 启动 | **不弹出登录页**；以游客身份进入看板；抽屉显示「游客」 |
| TC-BOOT-003 | P0 | **后端不可达不弹连接页** | 后端地址错误 | 1. 设错误地址 2. 重启 App | 仅 toast 提示「无法连接后端（离线模式可用）」；**不强制跳转连接设置页** |
| TC-BOOT-004 | P1 | 抽屉后端状态指示 | 后端正常 | 1. 打开抽屉 | 身份卡下显示**绿点 + 后端已连接**；断开后端 20 秒内变红点 |
| TC-BOOT-005 | P1 | 快速重复启动 | — | 1. 连续快速点图标 3 次 | 不产生多个实例；不崩溃 |
| TC-BOOT-006 | P2 | 后台恢复 | 已启动 | 1. 按 Home 2. 30 秒后回前台 | 状态保持，数据不丢失，无白屏重载 |

## C. 导航与抽屉（P0 · 历史问题高发区）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-NAV-001 | P0 | **左滑唤出抽屉（宽松区）** | 在看板页 | 1. 从屏幕左侧 40% 区域内任意位置向右滑 | 抽屉**一次就滑出**，不需要从边缘精确起手；跟手无卡顿 |
| TC-NAV-002 | P0 | **点遮罩关闭抽屉** | 抽屉已打开 | 1. 点抽屉右侧的空白区域 | **抽屉关闭**；**不触发主页任何按钮**（历史上会误触发「＋」） |
| TC-NAV-003 | P0 | **抽屉毛玻璃一致性** | 抽屉已打开 | 1. 在抽屉内部任意位置点击/长按/拖动 | 抽屉**始终保持毛玻璃半透明**，不出现「一点击就变纯色/变透明」 |
| TC-NAV-004 | P0 | **TabBar 点击反馈** | 任意页 | 1. 点 5 个 tab | 弹性缩放反馈（非方块高亮）；切换到对应页；当前 tab 图标放大 + 底部小圆点 |
| TC-NAV-005 | P0 | **中间凸起「状态」钮** | 任意页 | 1. 观察 TabBar 2. 点中间凸起钮 | 凸起圆钮样式正常；**文字「状态」在 TabBar 内**（不被挤出）；按压有弹性效果 |
| TC-NAV-006 | P0 | **顶部安全区** | 有刘海/挖孔设备 | 1. 打开「审核」「内容」等页 | 页面标题与系统状态栏**有间距**，不被遮挡、不重叠 |
| TC-NAV-007 | P1 | 子页返回 | 进入任一子页 | 1. 点左上「‹」 | 返回上一级；无残留遮罩、无卡死 |
| TC-NAV-008 | P1 | 连续快速切 tab | — | 1. 快速轮流点 5 个 tab 10 次 | 无白屏、无错位、无崩溃 |
| TC-NAV-009 | P2 | 抽屉滚动 | 抽屉内模块多 | 1. 上下滑动抽屉 | 滚动流畅；顶部身份卡/底部入口不重叠 |

## D. 零代码创建（P0 · 核心特性）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-WIZ-001 | P0 | 三个入口可用 | 已登录 | 1. 分别从①看板标题栏「✨零代码」②抽屉「✨零代码创建」③二次开发中心进入 | 三个入口都能打开向导，界面一致 |
| TC-WIZ-002 | P0 | 完整创建流程 | — | 1. 选「📊 数值卡片」→ 选数据源 → 点样例值 → 选格式化 → 命名 → 创建 | 向导进度条推进；创建成功，面板出现卡片；卡片显示真实数据 |
| TC-WIZ-003 | P0 | **向导可退出** | 向导已打开 | 1. 点右上「✕」 | 正常退出，**不保存**；再次进入是全新向导（不残留上次选择） |
| TC-WIZ-004 | P1 | 必填校验 | 向导第 2 步 | 1. 不选数据源直接点「下一步」 | 「下一步」置灰不可点，或有明确提示 |
| TC-WIZ-005 | P1 | 骨架屏与自动刷新 | 已创建模块 | 1. 观察卡片首次加载 2. 等待 30 秒 | 加载时显示**骨架屏动画**（非 ⏳）；30 秒后数据自动刷新 |
| TC-WIZ-006 | P1 | 错误重试 | 后端断开 | 1. 断开后端 2. 触发模块刷新 | 显示「⚠️ 错误 + ↻ 重试」按钮；点重试可重试 |
| TC-WIZ-007 | P2 | 模块计数 | 已创建 N 个 | 1. 进二次开发中心 | 标题显示「(N 个模块)」且与实际一致 |

## E. 密码管理（P0 · 安全关键）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-PWD-001 | P0 | 设置主密码 | 未设置 | 1. 我的 → 账号中心 → 输主密码 → 设置 | 状态变「🟢 已解锁」；提示可读写敏感数据 |
| TC-PWD-002 | P0 | **锁定态拒绝访问** | 已设主密码并锁定 | 1. 进「密码管理」 | 显示「🔒 保险库已锁定」；**不显示任何条目** |
| TC-PWD-003 | P0 | 解锁 | 已设置已锁定 | 1. 输正确主密码 → 解锁 | 解锁成功；密码列表可见 |
| TC-PWD-004 | P0 | 错误密码 | 已设置已锁定 | 1. 输错误密码 | 提示「密码错误」；**不解锁**、不泄露任何数据 |
| TC-PWD-005 | P0 | 新增密码 | 已解锁 | 1. 「＋ 新增密码」→ 填标题/密码 → 保存 | 提示「已加密保存」；列表出现新条目 |
| TC-PWD-006 | P0 | **密码生成器** | 新增表单已打开 | 1. 点「🎲」按钮 | 自动填入 16 位强密码；强度条显示「强」（绿色） |
| TC-PWD-007 | P0 | **强度条实时反馈** | 新增表单已打开 | 1. 依次输入 `123` / `P@ss1` / `Xk9#mP2$vL8@wQ4!` | 分别显示 弱(红) / 中(橙) / 强(绿) |
| TC-PWD-008 | P0 | 复制密码 | 已有条目 | 1. 点「复制密码」2. 到输入框粘贴 | 粘贴出正确密码；提示「15s 后剪贴板将清空」；15 秒后剪贴板被清空 |
| TC-PWD-009 | P1 | 搜索 | 多条密码 | 1. 输入标题/用户名/网址关键字 | 实时过滤；匹配项正确 |
| TC-PWD-010 | P1 | **分类折叠** | 有多个分类 | 1. 点分类标题 | 该分类下条目**折叠/展开**（历史上 CSS 选择器失效导致点了没反应） |
| TC-PWD-011 | P1 | 删除条目 | 已有条目 | 1. 点「删除」 | 条目消失；刷新后仍不存在 |
| TC-PWD-012 | P1 | 锁定后回访 | 已解锁并浏览过 | 1. 回账号中心锁定 2. 再进密码页 | 显示锁定态（不会仍显示旧数据） |
| TC-PWD-013 | P2 | XSS 防护 | 已解锁 | 1. 新增条目，标题填 `<img src=x onerror=alert(1)>` | 标题**按纯文本显示**，不弹窗、不渲染 HTML |

## F. 钱包（P1）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-WAL-001 | P1 | 记一笔支出 | 已解锁 vault | 1. 我的 → 钱包 → 选支出 → 金额 30 → 分类「餐饮」→ 记一笔 | 月度汇总「支出」增加 30；分类占比出现「餐饮」 |
| TC-WAL-002 | P1 | 记一笔收入 | 同上 | 1. 选收入 → 金额 5000 → 记一笔 | 「收入」增加；「结余」= 收入 − 支出（绿色） |
| TC-WAL-003 | P1 | **柱状图渲染** | 有多月数据 | 1. 观察图表 | 6 个月柱状图正常渲染；无数据月份显示最矮；金额标签可读 |
| TC-WAL-004 | P1 | **趋势箭头** | 有本月+上月数据 | 1. 观察趋势行 | 支出增加显示 ↑（红）、减少显示 ↓（绿）；无上月数据显示「首月」 |
| TC-WAL-005 | P1 | **分类占比条形图** | 有多笔支出 | 1. 观察分类区 | 水平条形图按金额降序；宽度按占比；金额右对齐 |
| TC-WAL-006 | P1 | 删除记录 | 已有记录 | 1. 点条目「✕」 | 记录删除；汇总/图表同步更新 |
| TC-WAL-007 | P2 | 边界金额 | — | 1. 金额填 `0` / 负数 / 超大数 / 非数字 | 有合理处理（拒绝或按规则记录），不崩溃、不显示 NaN |
| TC-WAL-008 | P2 | 锁定态访问 | vault 已锁定 | 1. 进钱包页 | 提示「🔒 请先在账号中心解锁」，不显示数据 |

## G. 相册（P1）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-ALB-001 | P1 | 打开相册 | — | 1. 我的 → 相册 | 页面正常；显示「＋ 选择照片」按钮与网格 |
| TC-ALB-002 | P1 | 选择照片预览 | — | 1. 点「＋ 选择照片」2. 选 1 张图片 | 系统文件选择器打开；选中后经压缩在网格显示缩略图 |
| TC-ALB-003 | P1 | 多选（最多 9 张） | — | 1. 一次选 9 张 | 9 张都显示；提示「已添加 9 张照片预览」 |
| TC-ALB-004 | P1 | 超大图压缩 | — | 1. 选一张 >5MB / 4000px 的图 | 压缩到 1920px 内；预览正常；**不卡死、不 OOM 崩溃** |
| TC-ALB-005 | P2 | 非图片文件 | — | 1. 尝试选非图片（若选择器允许） | 给出失败提示或忽略，不崩溃 |
| TC-ALB-006 | P2 | 权限拒绝 | — | 1. 拒绝存储/相册权限后重试选图 | 有明确提示，不闪退 |

## H. 通知中心（P2）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-NOT-001 | P2 | 打开通知中心 | 后端有 PENDING 数据 | 1. 状态栏 🔔 或菜单进入 | 显示待审评论/留言/说说评论分组与条数 |
| TC-NOT-002 | P2 | 无待审 | 后端无 PENDING | 1. 进入通知中心 | 显示「🎉 没有待处理的通知」 |
| TC-NOT-003 | P2 | 后端不可达 | 后端断开 | 1. 进入通知中心 | 显示友好错误提示（非白屏、非英文报错） |
| TC-NOT-004 | P2 | 红点数字 | 有待审 | 1. 观察状态栏铃铛 | 红点数字与实际待审数一致 |

## I. 弱网 / 断网 / 中断（P0 · 新修重点）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-NET-001 | P0 | **断网启动不卡死** | 关闭 WiFi + 数据 | 1. 启动 App | 15 秒内提示「网络连接失败 · 请检查后端地址与网络」；**不永久转圈**（这是本次修复的重点） |
| TC-NET-002 | P0 | **请求超时提示** | 后端人为慢响应（或加限速） | 1. 触发数据加载 | 15 秒后提示「请求超时（15s）· 请检查网络或后端地址」 |
| TC-NET-003 | P0 | 弱网加载 | 限速 2G/高延迟 | 1. 加载看板/内容 | 有加载态（骨架屏）；最终成功或给超时提示；不白屏 |
| TC-NET-004 | P0 | **WiFi ↔ 4G 切换** | 正常加载后 | 1. 切到 4G 2. 操作功能 | 请求自动走新网络；无需重启 App |
| TC-NET-005 | P1 | 断网后恢复 | 断网状态 | 1. 恢复网络 2. 下拉/重试 | 数据能重新加载；后端状态点转绿 |
| TC-NET-006 | P1 | **来电中断** | 操作中 | 1. 触发来电 2. 挂断后回 App | 状态保持；未提交的操作有草稿保留（编辑器场景） |
| TC-NET-007 | P1 | 锁屏/息屏恢复 | 已登录 | 1. 锁屏 5 分钟 2. 解锁回 App | 正常显示；若 vault 自动锁定则密码页要求重新解锁 |
| TC-NET-008 | P2 | 进程被杀恢复 | 已启动 | 1. 从最近任务划掉 2. 重新启动 | 冷启动成功；登录态与配置保留 |

## J. 主题与外观（P1）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-UI-001 | P1 | 暗色切换 | — | 1. 抽屉 → 「🌓 暗色」 | 全局切到暗色；文字仍清晰可读；毛玻璃质感保留 |
| TC-UI-002 | P1 | 暗色下 TabBar | 已切暗色 | 1. 观察 TabBar | 背景/文字对比度足够；凸起钮正常 |
| TC-UI-003 | P1 | 暗色下子页 | 已切暗色 | 1. 打开密码/钱包/相册 | 子页背景**不透明可读**（历史上子页过透导致看不清） |
| TC-UI-004 | P2 | 壁纸切换 | — | 1. 我的 → 壁纸 → 切换预设 | 背景更新；毛玻璃模糊效果跟随 |
| TC-UI-005 | P2 | 字体大小 | 系统设为最大字体 | 1. 启动 App | 布局不错乱、文字不截断（或可滚动查看） |

## K. 显示适配与兼容（P0）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-COMP-001 | P0 | 刘海屏适配 | 刘海/挖孔机型 | 1. 竖屏浏览全部主页面 | 标题/内容不被摄像头区域遮挡 |
| TC-COMP-002 | P0 | 长屏适配 | 20:9 等长屏 | 1. 浏览各页 | TabBar 与内容不重叠；底部无异常留白 |
| TC-COMP-003 | P0 | 小屏适配 | 5.0 寸左右 | 1. 浏览各页 | 卡片不溢出；TabBar 5 个 tab 正常显示 |
| TC-COMP-004 | P1 | 横屏 | — | 1. 旋转到横屏 | 不崩溃；布局可读（或有合理降级） |
| TC-COMP-005 | P1 | 手势导航条 | 全面屏手势 | 1. 底部上滑返回手势 | 不与 TabBar 冲突；TabBar 不被手势条遮挡 |
| TC-COMP-006 | P1 | 深色系统主题联动 | 系统切深色 | 1. 启动 App | App 主题与系统一致（或明确不联动，行为一致可预期） |

## L. 性能与稳定性（P1）

| 编号 | 优先级 | 用例标题 | 前置 | 操作步骤 | 预期结果 |
|---|---|---|---|---|---|
| TC-PERF-001 | P1 | 冷启动耗时 | — | 1. `device-qa -Action perf` 或秒表 | 3 秒内可交互（参考值，非硬性） |
| TC-PERF-002 | P1 | 内存占用 | — | 1. 浏览 10 分钟 2. 查内存 | 无持续增长（无内存泄漏）；稳定在合理区间 |
| TC-PERF-003 | P1 | 长时间运行 | — | 1. 前台停留 30 分钟并操作 | 无卡顿加剧、无闪退、无 ANR |
| TC-PERF-004 | P1 | 滚动流畅度 | 内容多 | 1. 快速滚动长列表 | 无明显掉帧（主观评估） |
| TC-PERF-005 | P1 | **后台无泄漏** | 已启动 | 1. 反复进出 App 10 次 | 不逐次变卡（验证已修的定时器泄漏问题） |

---

## 缺陷记录模板（发现问题时按此格式记录）

```markdown
### BUG-XXX：[模块] 操作路径 + 现象

- **关联用例**：TC-XXX-000
- **环境**：机型 / Android 版本 / App 版本 / 后端版本 / 网络
- **前置条件**：
- **复现步骤**：1. … 2. … 3. …
- **实际结果**：
- **预期结果**：
- **复现率**：必现 / 偶现(N/10)
- **证据**：截图/录屏（`device-qa -Action screenshot`）、日志（`device-qa -Action logs`）
- **严重程度**：致命（崩溃/数据丢失）/ 严重（主流程不可用）/ 一般（可绕过）/ 轻微（体验）
- **优先级**：紧急 / 高 / 中 / 低
- **定位层级**：前端 / 后端 / 数据 / 环境
- **补充**：是否首次出现、是否与特定机型相关
```

---

## 准出标准（判断能否发版）

| 指标 | 门槛 |
|---|---|
| P0 用例通过率 | **100%**（任何 P0 失败即阻断发版） |
| P1 用例通过率 | ≥ 95% |
| 致命/严重缺陷 | **0 个遗留** |
| 一般缺陷 | ≤ 3 个，且均有明确规避方案 |
| 崩溃率 | 测试期 0 次崩溃 / 0 次 ANR |

---

## 建议执行顺序

1. **A 安装** → 2. **B 启动** → 3. **C 导航**（历史问题最集中，优先验） → 4. **D 零代码**
5. **E 密码** → 6. **I 弱网**（本次修复重点） → 7. **K 适配** → 8. F/G/H/J/L

> 前三组（A/B/C）是用户实际使用的最短路径，也是历史上缺陷最集中的区域，建议优先执行。若时间有限，至少完成 **P0 全部 + C 组全部**。
